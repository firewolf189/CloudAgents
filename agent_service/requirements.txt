# agent_service dependencies
# agentscope 需要从官方仓库源码安装（PyPI 版本不含 app 模块）:
#   git clone https://github.com/agentscope-ai/agentscope.git src/agentscope
#   cd src/agentscope && pip install -e .[full]

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Auth
PyJWT>=2.8
bcrypt>=4.0

# Config
python-dotenv>=1.0
